# Kappa Estimation

```{R} 
#| echo: false
#| message: false
devtools::load_all("..")
library(purrr)
library(ggplot2)

trim <- function(x, lo = 0.01, hi = 0.99) x[x >= quantile(x, lo) & x <= quantile(x, hi)]

se_mse <- function(est, target) {
  c(
    bias = mean(est) - target,
    sd = sd(est),
    rmse = sqrt(mean((est - target)^2))
  )
}

```

## Motivation
The asymptotic variance of $\hat\rho$ is
$$
v^2 = \frac{1}{n}\!\left[(1-\rho^2)\!\left(\omega_x^2 + \omega_y^2 - 2\rho\,\omega_{xy}\right) + \frac{\rho^2}{4}(\kappa_{40}+2\kappa_{22}+\kappa_{04}) - \rho(\kappa_{31}+\kappa_{13}) + \tfrac{5}{2}(1-\rho^2)^2 + \kappa_{22}\right].
$$
Under $H_0: \rho = 0$ this reduces to
$$
\boxed{
v^2 = \frac{1}{n}\!\left(\omega_x^2 + \omega_y^2 + \kappa_{22} + \frac{5}{2}\right).
}
$$
Under Gaussianity $\kappa_{22} = 1$ and the null variance is $(\omega_x^2 + \omega_y^2 + 7/2)/n$. Without Gaussianity, $\kappa_{22}$ is unknown and **must be estimated** from data to form a valid test statistic.

## Estimating $\kappa_{22}$

There are multiple strategies to estimate $\kappa_{22}$. Let $s_i^{(k)} = (X_i - X_{i+k})(Y_i - Y_{i+k})$ and
$$
S_{xy,i} = \frac{s_i^{(1)} + s_{i+1}^{(1)} - s_i^{(2)}}{2},
$$
so that $E[S_{xy,i}] = \sigma_{xy}$ at every index regardless of change points. Replacing $Y$ with $X$ gives $S_{xx,i}$ (with $E[S_{xx,i}] = \sigma_x^2$), and similarly $S_{yy,i}$ (with $E[S_{yy,i}] = \sigma_y^2$). Their averages satisfy $\bar S_{xx} = \hat\sigma_x^2$, $\bar S_{yy} = \hat\sigma_y^2$, $\bar S_{xy} = \hat\sigma_{xy}$.

### $\operatorname{Var}(\hat{\sigma}_{xy})$-Based Estimator

We find that
$$
n\operatorname{Var}(\bar{S}_{xy}) = \kappa_{22}\sigma_x^2\sigma_y^2 + \frac{3\sigma_{xy}^2 + 4\sigma_{xy}\omega_{xy}}{2} + \left(\frac{\omega_x^2}{\sigma_x^2} + \frac{\omega_y^2}{\sigma_y^2} + \frac{5}{2}\right)\sigma_x^2\sigma_y^2.
$$

Solving for $\kappa_{22}$ and plugging in estimates gives
$$
\boxed{\hat\kappa_{22}^{(xy)} = \frac{n\widehat{\operatorname{Var}}(\bar{S}_{xy})}{\hat\sigma_x^2\hat\sigma_y^2} - \frac{3}{2}\frac{\hat\sigma_{xy}^2}{\hat\sigma_x^2\hat\sigma_y^2} - 2\frac{\hat\sigma_{xy}\hat\omega_{xy}}{\hat\sigma_x^2\hat\sigma_y^2} - \frac{\hat\omega_x^2}{\hat\sigma_x^2} - \frac{\hat\omega_y^2}{\hat\sigma_y^2} - \frac{5}{2}.}
$$

### $\operatorname{Cov}(\hat{\sigma}_x^2, \hat{\sigma}_y^2)$-Based Estimator

We find that
$$
n\operatorname{Cov}(\bar{S}_{xx}, \bar{S}_{yy}) = (\kappa_{22} - 1)\sigma_x^2\sigma_y^2 + 2\sigma_{xy}^2 + 4\sigma_{xy}\omega_{xy}.
$$

Solving for $\kappa_{22}$ and plugging in estimates gives
$$
\boxed{\hat\kappa_{22}^{(xx,yy)} = \frac{n\widehat{\operatorname{Cov}}(\bar{S}_{xx}, \bar{S}_{yy})}{\hat\sigma_x^2\hat\sigma_y^2} - 2\frac{\hat\sigma_{xy}^2}{\hat\sigma_x^2\hat\sigma_y^2} - 4\frac{\hat\sigma_{xy}\hat\omega_{xy}}{\hat\sigma_x^2\hat\sigma_y^2} + 1.}
$$

### Regression Estimator
Let us define 
$$
T_k^{(2)}(X, Y) = \sum_{i=1}^n (X_i - X_{i+k})^2 (Y_i - Y_{i+k})^2.
$$

It follows that
$$
\begin{aligned}
E[T_k^{(2)}(X, Y)]
&= 2n(\kappa_{22}-1)\sigma_x^2\sigma_y^2 + 8\sigma_{xy}T_k(\theta_x,\theta_y) + 4n\sigma_{xy}^2 \\
&\quad + T_k^{(2)}(\theta_x, \theta_y) + 2\sigma_y^2 T_k(\theta_x,\theta_x) + 2\sigma_x^2 T_k(\theta_y,\theta_y) + 4n\sigma_x^2\sigma_y^2.
\end{aligned}
$$

All $\theta$-dependent terms vanish in $2E[T_1^{(2)}] - E[T_2^{(2)}]$, giving
$$
2E[T_1^{(2)}(X,Y)] - E[T_2^{(2)}(X,Y)] = 2n(\kappa_{22}+1)\sigma_x^2\sigma_y^2 + 4n\sigma_{xy}^2.
$$

Solving for $\kappa_{22}$ and plugging in estimates gives
$$
\boxed{\hat\kappa_{22}^{r} = \frac{2T_1^{(2)}(X,Y) - T_2^{(2)}(X,Y)}{2n\hat\sigma_x^2\hat\sigma_y^2} - 2\frac{\hat\sigma_{xy}^2}{\hat\sigma_x^2\hat\sigma_y^2} - 1.}
$$


### Method of Moments Estimator
This method is inspired from the classical approach.

The classical method to estimate $\kappa_{22}$ is something like this
$$
\hat{\kappa}_{22} = \frac{E(\varepsilon_x^2 \varepsilon_y^2)}{\sigma_x^2 \sigma_y^2}.
$$

Of course, we cannot directly access $\varepsilon_x$ and $\varepsilon_y$ in the presence of change points. Instead, we use a proxy:  

$$
\hat{\kappa}_{22} = \frac{E(S_{xx} S_{yy}) }{\sigma_x^2 \sigma_y^2}. 
$$

Unfortunately, it is not so simple. If we do some analysis, we'll find that 

$$
\frac{1}{n}\sum_i E[S_{xx,i}\,S_{yy,i}] = \kappa_{22}\sigma_x^2\sigma_y^2 + 3\sigma_{xy}^2 + 4\sigma_{xy}\omega_{xy}.
$$

Therefore, we propose the method of moments estimator:
$$
\boxed{
  \hat{\kappa}_{22}^{\mathrm{MoM}} = \frac{\langle S_{xx}\,S_{yy}\rangle}{\hat\sigma_x^2\hat\sigma_y^2} - 3\frac{\hat\sigma_{xy}^2}{\hat\sigma_x^2\hat\sigma_y^2} - 4\frac{\hat\sigma_{xy}\hat\omega_{xy}}{\hat\sigma_x^2\hat\sigma_y^2}.
}
$$

## Numerical Results

### $\kappa_{22}$ Estimation

::: {.panel-tabset}

## No Change Points (Normal)

```{R k22-estimation-s1}
#| cache: true
#| echo: false
params <- scenario(1, r=0.5, err_type="normal")
sims <- map(1:10000, ~{
  X <- generate_data(params)
  list(
    k22_mom = ece.kappa(X, method = "mom")$k22,
    k22_mat = ece.kappa(X, method = "matrix")$k22,
    k22_reg = ece.kappa(X, method = "regress")$k22
  )
})
k22_mom <- map_dbl(sims, "k22_mom")
k22_mat <- map_dbl(sims, "k22_mat")
k22_reg <- map_dbl(sims, "k22_reg")
```


```{R}
#| echo: false
make_overlap_hist(
  sims = list(
    Regression   = k22_reg,
    "Var(Sxy)"   = k22_mat,
    MoM          = k22_mom
  ),
  xlab   = expression(hat(kappa)[22]),
  colors = c(Regression = "#E41A1C", "Var(Sxy)" = "#377EB8", MoM = "#4DAF4A")
) + ggplot2::labs(title = "Scenario 1, normal errors, n = 1000, ρ = 0.5") +
  ggplot2::geom_vline(xintercept = params$kappa$k22, linetype = "solid")
```

## Frequent Change Points (Exponential)

```{R k22-estimation-s2}
#| cache: true
#| echo: false
params <- scenario(2, L=4, r=0.5, err_type="exponential")
sims <- map(1:10000, ~{
  X <- generate_data(params)
  list(
    k22_mom = ece.kappa(X, method = "mom")$k22,
    k22_mat = ece.kappa(X, method = "matrix")$k22,
    k22_reg = ece.kappa(X, method = "regress")$k22
  )
})
k22_mom <- map_dbl(sims, "k22_mom")
k22_mat <- map_dbl(sims, "k22_mat")
k22_reg <- map_dbl(sims, "k22_reg")
```


```{R}
#| echo: false
make_overlap_hist(
  sims = list(
    Regression   = k22_reg,
    "Var(Sxy)"   = k22_mat,
    MoM          = k22_mom
  ),
  xlab   = expression(hat(kappa)[22]),
  colors = c(Regression = "#E41A1C", "Var(Sxy)" = "#377EB8", MoM = "#4DAF4A")
) + ggplot2::labs(title = "Scenario 2 (L=4), exponential errors, n = 1000, ρ = 0.5") +
  ggplot2::geom_vline(xintercept = params$kappa$k22, linetype = "solid")
```

::: 

```{R k22-bias-var}
#| cache: true
#| echo: false
param_grid <- tribble(
  ~label,                        ~scen, ~r,   ~err_type,      ~amp, ~L,
  "Scenario 1, Normal",           1,     0,    "normal",       1,    NA,
  "Scenario 1, Exponential",      1,     0,    "exponential",  1,    NA,
  "Scenario 1 (r = 0.5), Exp.",  1,     0.5,  "exponential",  1,    NA,
  "Scenario 2, Normal",           2,     0,    "normal",       1,    4,
  "Scenario 2, Exponential",      2,     0,    "exponential",  1,    4,
  "Scenario 2 (amp = 2), Exp.",  2,     0,    "exponential",  2,    4
)

simulate_k22 <- function(params, N = 1000) {
  map(1:N, ~ {
    X <- generate_data(params)
    list(
      regress = ece.kappa(X, method = "regress")$k22,
      matrix  = ece.kappa(X, method = "matrix")$k22,
      mom     = ece.kappa(X, method = "mom")$k22
    )
  })
}

k22_grid <- pmap(param_grid, function(label, scen, r, err_type, amp, L) {
  params <- if (is.na(L)) {
    scenario(scen, r = r, err_type = err_type, amp = amp)
  } else {
    scenario(scen, r = r, err_type = err_type, amp = amp, L = L)
  }
  sim <- simulate_k22(params)
  list(
    label = label,
    true  = params$kappa$k22,
    sim   = sim
  )
})
```

```{R}
#| echo: false
ci_df <- map_dfr(k22_grid, \(g) {
  tibble(
    scenario = g$label,
    method   = c("Regression", "Var(Sxy)", "MoM"),
    bias     = c(mean(map_dbl(g$sim, "regress")),
                 mean(map_dbl(g$sim, "matrix")),
                 mean(map_dbl(g$sim, "mom"))) - g$true,
    sd       = c(sd(map_dbl(g$sim, "regress")),
                 sd(map_dbl(g$sim, "matrix")),
                 sd(map_dbl(g$sim, "mom")))
  )
}) |>
  mutate(
    lo       = bias - 1.96 * sd,
    hi       = bias + 1.96 * sd,
    scenario = factor(scenario, levels = map_chr(k22_grid, "label")),
    method   = factor(method, levels = c("Regression", "Var(Sxy)", "MoM"))
  )

ggplot(ci_df, aes(x = scenario, y = bias, color = method, group = method)) +
  geom_hline(yintercept = 0, linetype = "dashed", color = "gray50") +
  geom_pointrange(aes(ymin = lo, ymax = hi),
                  position = position_dodge(width = 0.4)) +
  scale_color_brewer(palette = "Set1", name = NULL) +
  labs(x = NULL, y = expression(hat(kappa)[22] - kappa[22]),
       title = expression("Bias and variance of " * hat(kappa)[22] * " estimators")) +
  theme_minimal() +
  theme(
    axis.text.x = element_text(angle = 90, hjust = 1, vjust = 0.5),
    panel.grid.major.y = element_blank(),
    panel.grid.minor.y = element_blank()
  )
```

### Null SE Estimation
While it seems pretty clear cut that the MoM estimator is the most accurate, it's not entirely clear which method is the most powerful. 


::: {.panel-tabset}

## No Change Points (Normal Error, $\rho=0$)

```{R null-se-estimation-s1}
#| cache: true
#| echo: false
params <- scenario(1, err_type="normal")
sims <- map(1:10000, ~{
  X <- generate_data(params)
  list(
    se_mom = test.diag(X, method="z.test.null")$se,
    se_mat = test.diag(X, method="z.test.null.matrix")$se,
    se_reg = test.diag(X, method="z.test.null.regress")$se,
    se_oracle = test.diag(X, method="z.test.null.oracle")$se
  )
})
se_mom <- map_dbl(sims, "se_mom")
se_mat <- map_dbl(sims, "se_mat")
se_reg <- map_dbl(sims, "se_reg")
se_oracle <- map_dbl(sims, "se_oracle")
```

```{R}
#| echo: false
make_overlap_hist(
  sims = list(
    Regression = trim(se_reg),
    "Var(Sxy)" = trim(se_mat),
    MoM        = trim(se_mom),
    Oracle     = trim(se_oracle)
  ),
  xlab   = expression(hat(v)),
  colors = c(Regression = "#E41A1C", "Var(Sxy)" = "#377EB8", MoM = "#4DAF4A", Oracle = "#984EA3")
) + ggplot2::labs(title = "Scenario 1, normal errors, n = 1000, ρ = 0") +
  ggplot2::geom_vline(xintercept = ece.cor.se.formula(params), linetype = "solid")
```

```{R}
#| echo: false
target <- ece.cor.se.formula(params)

rbind(
  Oracle     = se_mse(se_oracle, target),
  "Var(Sxy)" = se_mse(se_mat,    target),
  Regression = se_mse(se_reg,    target),
  MoM        = se_mse(se_mom,    target)
  ) |>
  as.data.frame() |>
  round(5) |> 
  arrange(rmse)
```

## Frequent Change Points (L=4) - Exponential Error ($\rho=0$)

```{R null-se-estimation-s3}
#| cache: true
#| echo: false
params <- scenario(2, L=4, r=0, err_type="exponential")
sims <- map(1:10000, ~{
  X <- generate_data(params)
  list(
    se_mom = test.diag(X, method="z.test.null")$se,
    se_mat = test.diag(X, method="z.test.null.matrix")$se,
    se_reg = test.diag(X, method="z.test.null.regress")$se,
    se_oracle = test.diag(X, method="z.test.null.oracle")$se
  )
})
se_mom <- map_dbl(sims, "se_mom")
se_mat <- map_dbl(sims, "se_mat")
se_reg <- map_dbl(sims, "se_reg")
se_oracle <- map_dbl(sims, "se_oracle")
```

```{R}
#| echo: false
make_overlap_hist(
  sims = list(
    Regression = trim(se_reg),
    "Var(Sxy)" = trim(se_mat),
    MoM        = trim(se_mom),
    Oracle     = trim(se_oracle)
  ),
  xlab   = expression(hat(v)),
  colors = c(Regression = "#E41A1C", "Var(Sxy)" = "#377EB8", MoM = "#4DAF4A", Oracle = "#984EA3")
) + ggplot2::labs(title = "Scenario 2 (L=4), exponential errors, n = 1000, ρ = 0.5") +
  ggplot2::geom_vline(xintercept = ece.cor.se.formula(params), linetype = "solid")
```

```{R}
#| echo: false
target <- ece.cor.se.formula(params)

rbind(
  Oracle     = se_mse(se_oracle, target),
  "Var(Sxy)" = se_mse(se_mat,    target),
  Regression = se_mse(se_reg,    target),
  MoM        = se_mse(se_mom,    target)
  ) |>
  as.data.frame() |>
  round(5) |> 
  arrange(rmse)
```

## No Change Points (Normal Error, $\rho=0.5$)

```{R null-se-estimation-s4}
#| cache: true
#| echo: false
params <- scenario(1, r=0.5, err_type="normal")
sims <- map(1:10000, ~{
  X <- generate_data(params)
  list(
    se_mom = test.diag(X, method="z.test.null")$se,
    se_mat = test.diag(X, method="z.test.null.matrix")$se,
    se_reg = test.diag(X, method="z.test.null.regress")$se,
    se_oracle = test.diag(X, method="z.test.null.oracle")$se
  )
})
se_mom <- map_dbl(sims, "se_mom")
se_mat <- map_dbl(sims, "se_mat")
se_reg <- map_dbl(sims, "se_reg")
se_oracle <- map_dbl(sims, "se_oracle")
```

```{R}
#| echo: false
make_overlap_hist(
  sims = list(
    Regression = trim(se_reg),
    "Var(Sxy)" = trim(se_mat),
    MoM        = trim(se_mom),
    Oracle     = trim(se_oracle)
  ),
  xlab   = expression(hat(v)),
  colors = c(Regression = "#E41A1C", "Var(Sxy)" = "#377EB8", MoM = "#4DAF4A", Oracle = "#984EA3")
) + ggplot2::labs(title = "Scenario 1, normal errors, n = 1000, ρ = 0.5") +
  ggplot2::geom_vline(xintercept = ece.cor.se.formula(params), linetype = "solid")
```

```{R}
#| echo: false
target <- ece.cor.se.formula(params)

rbind(
  Oracle     = se_mse(se_oracle, target),
  "Var(Sxy)" = se_mse(se_mat,    target),
  Regression = se_mse(se_reg,    target),
  MoM        = se_mse(se_mom,    target)
  ) |>
  as.data.frame() |>
  round(5) |> 
  arrange(rmse)
```

## Frequent Change Points (L=4) - Exponential Error ($\rho=0.5$)

```{R null-se-estimation-s2}
#| cache: true
#| echo: false
params <- scenario(2, L=4, r=0.5, err_type="exponential")
sims <- map(1:10000, ~{
  X <- generate_data(params)
  list(
    se_mom = test.diag(X, method="z.test.null")$se,
    se_mat = test.diag(X, method="z.test.null.matrix")$se,
    se_reg = test.diag(X, method="z.test.null.regress")$se,
    se_oracle = test.diag(X, method="z.test.null.oracle")$se
  )
})
se_mom <- map_dbl(sims, "se_mom")
se_mat <- map_dbl(sims, "se_mat")
se_reg <- map_dbl(sims, "se_reg")
se_oracle <- map_dbl(sims, "se_oracle")
```

```{R}
#| echo: false
make_overlap_hist(
  sims = list(
    Regression = trim(se_reg),
    "Var(Sxy)" = trim(se_mat),
    MoM        = trim(se_mom),
    Oracle     = trim(se_oracle)
  ),
  xlab   = expression(hat(v)),
  colors = c(Regression = "#E41A1C", "Var(Sxy)" = "#377EB8", MoM = "#4DAF4A", Oracle = "#984EA3")
) + ggplot2::labs(title = "Scenario 2 (L=4), exponential errors, n = 1000, ρ = 0.5") +
  ggplot2::geom_vline(xintercept = ece.cor.se.formula(params), linetype = "solid")
```

```{R}
#| echo: false
target <- ece.cor.se.formula(params)

rbind(
  Oracle     = se_mse(se_oracle, target),
  "Var(Sxy)" = se_mse(se_mat,    target),
  Regression = se_mse(se_reg,    target),
  MoM        = se_mse(se_mom,    target)
  ) |>
  as.data.frame() |>
  round(5) |> 
  arrange(rmse)
```

## No Change Points (Normal Error, $\rho=0.5$, small $n$)

```{R null-se-estimation-s5}
#| cache: true
#| echo: false
params <- scenario(1, n=200, r=0.5, err_type="normal")
sims <- map(1:10000, ~{
  X <- generate_data(params)
  list(
    se_mom = test.diag(X, method="z.test.null")$se,
    se_mat = test.diag(X, method="z.test.null.matrix")$se,
    se_reg = test.diag(X, method="z.test.null.regress")$se,
    se_oracle = test.diag(X, method="z.test.null.oracle")$se
  )
})
se_mom <- map_dbl(sims, "se_mom")
se_mat <- map_dbl(sims, "se_mat")
se_reg <- map_dbl(sims, "se_reg")
se_oracle <- map_dbl(sims, "se_oracle")
```

```{R}
#| echo: false
make_overlap_hist(
  sims = list(
    Regression = trim(se_reg),
    "Var(Sxy)" = trim(se_mat),
    MoM        = trim(se_mom),
    Oracle     = trim(se_oracle)
  ),
  xlab   = expression(hat(v)),
  colors = c(Regression = "#E41A1C", "Var(Sxy)" = "#377EB8", MoM = "#4DAF4A", Oracle = "#984EA3")
) + ggplot2::labs(title = "Scenario 1, normal errors, n = 200, ρ = 0.5") +
  ggplot2::geom_vline(xintercept = ece.cor.se.formula(params), linetype = "solid")
```

```{R}
#| echo: false
target <- ece.cor.se.formula(params)

rbind(
  Oracle     = se_mse(se_oracle, target),
  "Var(Sxy)" = se_mse(se_mat,    target),
  Regression = se_mse(se_reg,    target),
  MoM        = se_mse(se_mom,    target)
  ) |>
  as.data.frame() |>
  round(5) |> 
  arrange(rmse)
```

## No Change Points (Normal Error, $\rho=0.1$)

```{R null-se-estimation-s6}
#| cache: true
#| echo: false
params <- scenario(1, n=500, r=0.1, err_type="normal")
sims <- map(1:10000, ~{
  X <- generate_data(params)
  list(
    se_mom = test.diag(X, method="z.test.null")$se,
    se_mat = test.diag(X, method="z.test.null.matrix")$se,
    se_reg = test.diag(X, method="z.test.null.regress")$se,
    se_oracle = test.diag(X, method="z.test.null.oracle")$se
  )
})
se_mom <- map_dbl(sims, "se_mom")
se_mat <- map_dbl(sims, "se_mat")
se_reg <- map_dbl(sims, "se_reg")
se_oracle <- map_dbl(sims, "se_oracle")
```

```{R}
#| echo: false
make_overlap_hist(
  sims = list(
    Regression = trim(se_reg),
    "Var(Sxy)" = trim(se_mat),
    MoM        = trim(se_mom),
    Oracle     = trim(se_oracle)
  ),
  xlab   = expression(hat(v)),
  colors = c(Regression = "#E41A1C", "Var(Sxy)" = "#377EB8", MoM = "#4DAF4A", Oracle = "#984EA3")
) + ggplot2::labs(title = "Scenario 1, normal errors, n = 500, ρ = 0.1") +
  ggplot2::geom_vline(xintercept = ece.cor.se.formula(params), linetype = "solid")
```

```{R}
#| echo: false
target <- ece.cor.se.formula(params)

rbind(
  Oracle     = se_mse(se_oracle, target),
  "Var(Sxy)" = se_mse(se_mat,    target),
  Regression = se_mse(se_reg,    target),
  MoM        = se_mse(se_mom,    target)
  ) |>
  as.data.frame() |>
  round(5) |> 
  arrange(rmse)
```

::: 

```{R}
#| echo: false
sim_decomp <- function(params, method, n_sim=10000){
  set.seed(1)
  sims <- map(1:n_sim, ~{

    # Generate Data
    X <- generate_data(params)
    x <- X[, 1]; y <- X[, 2]

    # Estimate Complexity Parameters
    wx2 <- ece.complexity(x)
    wy2 <- ece.complexity(y)

    # Estimate k22
    if(method == "oracle"){
      kappa <- params$kappa
    }else{
      kappa <- ece.kappa(X, method=method)
    }

    # Run Test
    z.test <- z_test(X, kappa=kappa, rho=0)

    # Return 
    list(
      rxy = z.test$estimate, 
      pval = z.test$p.value,
      se = z.test$se,
      w = wx2 + wy2,
      k22 = kappa$k22
    )
  })
  list(
    rxy = map_dbl(sims, "rxy"),
    pval = map_dbl(sims, "pval"),
    se = map_dbl(sims, "se"),
    w = map_dbl(sims, "w"),
    k22 = map_dbl(sims, "k22")
  )
}

panel_hist <- function(vals_list, true_val = NULL, xlab = "", colors, means = NULL, ...) {
  df <- imap_dfr(vals_list, ~ tibble(x = .x, method = .y))
  p <- ggplot(df, aes(x = x, fill = method)) +
    geom_histogram(aes(y = after_stat(density)),
                   position = "identity", color = "white", alpha = 0.5, ...) +
    scale_fill_manual(values = colors, name = NULL, drop = FALSE) +
    labs(x = xlab, y = NULL) +
    theme_minimal()
  if (!is.null(true_val))
    p <- p + geom_vline(xintercept = true_val, linetype = "solid")
  if (!is.null(means))
    for (nm in names(means))
      p <- p + geom_vline(xintercept = means[[nm]], color = colors[nm],
                          linetype = "dashed", linewidth = 0.7)
  p
}

plot_decomp <- function(sims, params, colors) {
  target_se  <- ece.cor.se.formula(params)
  target_k22 <- params$kappa$k22
  target_w   <- ece.complexity(params$h[,1]) + ece.complexity(params$h[,2])

  extract  <- function(var) map(sims, var)
  sims_est <- sims[names(sims) != "Oracle"]
  se_means <- map(sims, ~ mean(.x$se))

  patchwork::wrap_plots(
    panel_hist(extract("pval"),      xlab = "p-value",                                                                                             colors = colors, breaks = seq(0, 1, by = 0.1)),
    panel_hist(extract("se"),        target_se,  expression(hat(v) == sqrt((hat(omega)[x]^2 + hat(omega)[y]^2 + hat(kappa)[22] + frac(5,2)) / n)), colors = colors, means = se_means, bins = 40),
    panel_hist(map(sims_est, "w"),   target_w,   expression(hat(omega)[x]^2 + hat(omega)[y]^2),                                                    colors = colors, bins = 40),
    panel_hist(map(sims_est, "k22"), target_k22, expression(hat(kappa)[22]),                                                                        colors = colors, bins = 40),
    nrow = 2
  ) +
    patchwork::plot_layout(guides = "collect") &
    theme(legend.position = "bottom")
}
```

:::{.panel-tabset}

## Scenario 1 (n=200, r=0, normal error)
```{R sim-decomp}
#| echo: false
#| cache: true

# Run Simulations
params <- scenario(1, n=200, r=0, err_type="normal")
sim_mom <- sim_decomp(params, method="mom")
sim_mat <- sim_decomp(params, method="matrix")
sim_reg <- sim_decomp(params, method="regress")
sim_oracle <- sim_decomp(params, method="oracle") 
```

```{R}
#| echo: false

sims <- list(
  MoM        = sim_mom,
  "Var(Sxy)" = sim_mat,
  Regression = sim_reg,
  Oracle     = sim_oracle
)
colors <- c(MoM = "#4DAF4A", "Var(Sxy)" = "#377EB8", Regression = "#E41A1C", Oracle = "#984EA3")

plot_decomp(sims, params, colors)
```

## Scenario 1 (n=200, r=0.2, normal error)
```{R sim-decomp-2}
#| echo: false
#| cache: true

# Run Simulations
params <- scenario(1, n=200, r=0.2, err_type="normal")
sim_mom <- sim_decomp(params, method="mom")
sim_mat <- sim_decomp(params, method="matrix")
sim_reg <- sim_decomp(params, method="regress")
sim_oracle <- sim_decomp(params, method="oracle") 
```

```{R}
#| echo: false

sims <- list(
  MoM        = sim_mom,
  "Var(Sxy)" = sim_mat,
  Regression = sim_reg,
  Oracle     = sim_oracle
)
colors <- c(MoM = "#4DAF4A", "Var(Sxy)" = "#377EB8", Regression = "#E41A1C", Oracle = "#984EA3")

plot_decomp(sims, params, colors)
```

## Scenario 1 (n=1000, r=0.5, normal error)
```{R sim-decomp-3}
#| echo: false
#| cache: true

# Run Simulations
params <- scenario(1, n=1000, r=0.5, err_type="normal")
sim_mom <- sim_decomp(params, method="mom")
sim_mat <- sim_decomp(params, method="matrix")
sim_reg <- sim_decomp(params, method="regress")
sim_oracle <- sim_decomp(params, method="oracle") 
```

```{R}
#| echo: false

sims <- list(
  MoM        = sim_mom,
  "Var(Sxy)" = sim_mat,
  Regression = sim_reg,
  Oracle     = sim_oracle
)
colors <- c(MoM = "#4DAF4A", "Var(Sxy)" = "#377EB8", Regression = "#E41A1C", Oracle = "#984EA3")

plot_decomp(sims, params, colors)
```

::: 

